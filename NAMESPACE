importFrom("graphics", plot)

import("methods")
import("Formula")
import("survival")
import("coin")

export(kaps.control, kaps, kapsNews, count.mindat, km.curve)

exportClasses(kaps)
exportMethods(show, print, plot, predict, summary) 
